# Common build configurations
build --show_result=1
build --symlink_prefix=bazel-

# Python toolchain configuration
# build --python_version=3.13
# build --repo_mapping=rules_python=rules_python

# Rust toolchain configuration
# build --repo_mapping=rules_rust=rules_rust
